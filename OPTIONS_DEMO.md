# Disposable: cortex PR-options live demo

This PR exists to demo the interactive options checkboxes on the cortex
consolidated comment. It will be closed unmerged.

```ts
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
```
